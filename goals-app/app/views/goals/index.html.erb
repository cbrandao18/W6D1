<h1>Goals</h1>

<ul>
    <% @goals.each do |goal|%>
        <li>
            <a href="<%= goal_url(goal) %>"> 
                <%= goal.title %>
            </a>
            <p>
                <%= goal.body %>
            </p>
        </li>
    <% end %>
</ul>